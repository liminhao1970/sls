SET NAMES UTF8;
DROP DATABASE IF EXISTS login;
CREATE DATABASE tmooc CHARSET=UTF8;
USE login;

CREATE TABLE users(
  uid INT PRIMARY KEY AUTO_INCREMENT,
  email VARCHAR(64),
  pwd VARCHAR(64),
  nickname VARCHAR(64),
  regtime LONG
);
